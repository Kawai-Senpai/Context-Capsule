node_modules/
*.log
.DS_Store
Thumbs.db

# Local capture output and generated native-host manifests
companion/native-host.manifest.json
captures/

# Design document — local only, not distributed
docs/PLAN.md

# Signing key — losing this loses update rights; never commit it
keys/
dist/
